# Development environment overrides
# Only values that differ from values.yaml belong here.
# Env-specific ingress, resources, autoscaling, etc. live in the config repo.

image:
  backend:
    repository: medical-appointment-backend
    tag: local
    pullPolicy: Never
  frontend:
    repository: medical-appointment-frontend
    tag: local
    pullPolicy: Never